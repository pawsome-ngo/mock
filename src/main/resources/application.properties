spring.application.name=spring-mock
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:h2:file:./data/mock-api-db

# Defines the path to the external folder for JSON mock files.
# Using "./" makes it relative to your project's root directory.
mock.files.path=./mock-definitions/